const welcome = document.getElementById("welcome");

const firstName = "Anthony";
const greeting = "welcome back!";

welcome.innerText = firstName + ", " + greeting;

welcome.innerText += " *waving emoji*";
